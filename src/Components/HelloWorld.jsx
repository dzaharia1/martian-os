import React from 'react';
import styled from 'styled-components';

const AppContainer = styled.div`
    padding: 24px;
    color: #fff;
`;

const HelloWorld = () => {
    return <AppContainer>Hello from the HelloWorld App!</AppContainer>;
};

export default HelloWorld;
